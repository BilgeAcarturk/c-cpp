#include <stdio.h>

int main(){
    int num, n=1;
    printf("Enter an integer: \n");
    scanf("%d", &num);
    for (int i=1; i<=num; i++){
        n= n*i;
    }
    printf("Factorial: %d", n);
}